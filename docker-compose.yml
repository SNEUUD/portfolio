services:
  portfolio:
    build: .
    container_name: portfolio
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
    networks:
      - nginx_network

# For nginx-proxy
networks:
  nginx_network:
    external: true
    name: nginx_default